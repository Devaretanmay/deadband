[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "loopless-crewai"
version = "0.1.0"
description = "Loopless integration for CrewAI"
requires-python = ">=3.9"
dependencies = ["loopless>=0.1.0", "crewai>=0.70.0"]
