[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "loopless-langgraph"
version = "0.1.0"
description = "Loopless integration for LangGraph"
requires-python = ">=3.9"
dependencies = ["loopless>=0.1.0", "langgraph>=0.2.0"]
