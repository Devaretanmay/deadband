[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "loopless-openai-agents"
version = "0.1.0"
description = "Loopless integration for OpenAI Agents SDK"
requires-python = ">=3.9"
dependencies = ["loopless>=0.1.0", "openai-agents>=0.1.0"]
